{
  "name": "Octobercmspond",
  "version": "0.0.1",
  "description": "October CMS Pond",
  "devDependencies": {
    "fs-jetpack": "^1.1.0",
    "gulp": "^3.9.1",
    "gulp-util": "^3.0.8",
    "nw": "^0.23.6-sdk-1",
    "nw-builder": "^3.4.1",
    "q": "^1.5.0"
  },
  "author": "Alexey Bobkov <aleksey.bobkov@gmail.com>",
  "contributors": [
    "Samuel Georges <daftspunky@gmail.com>"
  ],
  "scripts": {
    "postinstall": "cd ./app  &&  npm install && cd ../nw-normal && npm install",
    "dev": "node ./tasks/dev",
    "watch": "cd ./app && test -d dist/assets || mkdir -p dist/assets && cp -a src/assets/. dist/assets && watchify -t vueify -p [ vueify/plugins/extract-css -o dist/main.css ] -e src/js/main.js -o dist/main.js -v",
    "copy-assets": "cd ./app && test -d dist/assets || mkdir -p dist/assets && cp -a src/assets/. dist/assets",
    "release": "./node_modules/.bin/gulp release --env=production"
  },
  "optionalDependencies": {
    "appdmg": "^0.4.5"
  }
}
